<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= action === "insert" ? "Insert Appointment" : "Edit Appointment" %></title>

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">

    <style>
        body {
            background-color: #121212;
            color: #ffffff;
        }

        .form-container {
            max-width: 600px;
            margin: 50px auto;
            background: #1e1e1e;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.5);
        }

        .form-container h1 {
            font-size: 24px;
            text-align: center;
            margin-bottom: 20px;
        }

        label {
            font-weight: bold;
            color: #ccc;
        }

        input, select, textarea {
            background-color: #222;
            color: #fff;
            border: 1px solid #444;
        }

        input:focus, select:focus, textarea:focus {
            outline: none;
            box-shadow: 0 0 5px #007bff;
            border-color: #007bff;
        }

        button {
            width: 100%;
            background-color: #007bff;
            border: none;
            padding: 10px;
            font-size: 16px;
            border-radius: 4px;
            color: white;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #0056b3;
        }

        .btn-secondary {
            background-color: #444;
        }

        .btn-secondary:hover {
            background-color: #333;
        }

        .form-footer {
            text-align: center;
            margin-top: 20px;
        }

        .form-footer a {
            color: #007bff;
            text-decoration: none;
        }

        .form-footer a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="form-container">
            <h1><%= action === "insert" ? "Add New Appointment" : "Edit Appointment" %></h1>
            <form action="/calendar/<%= action %><%= action === 'edit' ? `/${data.ApptID}` : '' %>" method="POST">
                <!-- Appointment Subject -->
                <div class="mb-3">
                    <label for="ApptSubject" class="form-label">Subject</label>
                    <input type="text" id="ApptSubject" name="ApptSubject" class="form-control" value="<%= data.ApptSubject || '' %>" required>
                </div>

                <!-- Appointment Location -->
                <div class="mb-3">
                    <label for="ApptLocation" class="form-label">Location</label>
                    <input type="text" id="ApptLocation" name="ApptLocation" class="form-control" value="<%= data.ApptLocation || '' %>">
                </div>

                <!-- Appointment Start -->
                <div class="mb-3">
                    <label for="ApptStart" class="form-label">Start Time</label>
                    <input type="datetime-local" id="ApptStart" name="ApptStart" class="form-control" value="<%= data.ApptStart || '' %>" required>
                </div>

                <!-- Appointment End -->
                <div class="mb-3">
                    <label for="ApptEnd" class="form-label">End Time</label>
                    <input type="datetime-local" id="ApptEnd" name="ApptEnd" class="form-control" value="<%= data.ApptEnd || '' %>" required>
                </div>

                <!-- Notes -->
                <div class="mb-3">
                    <label for="ApptNotes" class="form-label">Notes</label>
                    <textarea id="ApptNotes" name="ApptNotes" class="form-control" rows="4"><%= data.ApptNotes || '' %></textarea>
                </div>

                <!-- Priority -->
                <div class="mb-3">
                    <label for="Priority" class="form-label">Priority</label>
                    <input type="int" id="Priority" name="Priority" class="form-control" value="<%= data.Priority || '' %>">
                </div>

                <!-- Submit Button -->
                <button type="submit" class="btn btn-primary"><%= action === "insert" ? "Add Appointment" : "Save Changes" %></button>

                <!-- Cancel Button -->
                <a href="/calendar" class="btn btn-secondary mt-3">Cancel</a>
            </form>
        </div>
    </div>
</body>

</html>
