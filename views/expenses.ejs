<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expenses</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <h1 class="text-center">Expenses</h1>

    <!-- Table with Bootstrap styling -->
    <div class="container">
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>VoucherDate</th>
                    <th>AccountName</th>
                    <th>AccountType</th>
                    <th>DebitAmount</th>
                    <th>CreditAmount</th>
                    <th>run_asset</th>
                    <th>Narration</th>
                </tr>
            </thead>
            <tbody>
                <!-- Iterate through the database array -->
                <% for (let data of database) { %>
                    <tr>
                        <td><%= data.VoucherDate %></td>
                        <td><%= data.AccountName %></td>
                        <td><%= data.AccountType %></td>
                        <td><%= data.DebitAmount %></td>
                        <td><%= data.CreditAmount %></td>
                        <td><%= data.run_asset %></td>
                        <td><%= data.Narration %></td>


                        <!-- <td>
                            <form method="get" action="/user/calendar/edit">
                                <button type="submit">Edit</button>
                            </form>
                        </td> -->
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</body>

</html>
