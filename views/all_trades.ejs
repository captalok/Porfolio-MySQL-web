<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Trades</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <h1 class="text-center">All Trades</h1>

    <!-- Table with Bootstrap styling -->
    <div class="container">
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>TradeID</th>
                    <th>ScripName</th>
                    <th>BuyDate</th>
                    <th>BuyQty</th>
                    <th>BuyPrice</th>
                    <th>SellPrice</th>
                    <th>Brokerage</th>
                    <th>GrossProfit</th>
                    <th>DepositWithdrawal</th>
                    <th>Pips</th>
                    
                </tr>
            </thead>
            <tbody>
                <!-- Iterate through the database array -->
                <% for (let data of database) { %>
                    <tr>
                        <td><%= data.TradeID %></td>
                        <td><%= data.ScripName %></td>
                        <td><%= data.BuyDate %></td>
                        <td><%= data.BuyQty %></td>
                        <td><%= data.BuyPrice %></td>
                        <td><%= data.SellPrice %></td>
                        <td><%= data.Brokerage %></td>
                        <td><%= data.GrossProfit %></td>
                        <td><%= data.DepositWithdrawal %></td>
                        <td><%= data.Pips %></td>
                        


                        <!-- <td>
                            <form method="get" action="/user/calendar/edit">
                                <button type="submit">Edit</button>
                            </form>
                        </td> -->
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</body>

</html>
